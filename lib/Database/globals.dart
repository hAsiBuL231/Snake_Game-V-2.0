library globals;

//var num = new GameSettingsState().gColumn;

const int grow = 20;
int gColumn = 38;
int gLevel = 150;
int gScore = 0;
//String gMode = gMode == 1 ? 'Closed' : 'Opened'; // Closed or Opened
String gMode = 'Closed'; // Closed or Opened
String gSystem = 'Touch'; // 'Button'